const shoping= {
    mobile: 2,
    pc: 1,
    keyboard: 3,
    mouse: 1,
    pen: 20,
    noteBook: 10
}
console.log(shoping);

// all the keys of object:
var keys =  Object.keys(shoping);
console.log(keys);

// all the values of object:
var values =  Object.values(shoping);
console.log(values);